<Page x:ConnectionId='1'
    x:Class="VS2017EjercicioClaroV.ClaroMusicaEjercicio"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VS2017EjercicioClaroV"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="TodasLasListas_DefaultItemTemplate" >
            <Grid Height="150"
                  Width="120"
                  Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <StackPanel>
                    <Image Source="{Binding Image}" />
                    <!-- <TextBox Text="{Binding Title, Mode=TwoWay}" /> --> <!-- testing bind MVVM dos vias -->
                    <TextBlock Text="{Binding Title}" />
                </StackPanel>
            </Grid>
            
         </DataTemplate>
    </Page.Resources>
        
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="34*" />
            <ColumnDefinition Width="33*" />
            <ColumnDefinition Width="33*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*" />
            <RowDefinition Height="10*" />
            <RowDefinition Height="80*"/>
        </Grid.RowDefinitions>


        
        
        
        <!-- ======================= HEADER ======================= -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0"  >
            <Image HorizontalAlignment="Stretch" Source="../Images/ClaroImg.png" />
        </StackPanel>
        

        <TextBox x:ConnectionId='2' x:Name="textBoxFiltro" Grid.Row="0"  Grid.Column="1" HorizontalAlignment="Right" Width="300" Height="20" />
        <Button x:ConnectionId='3' Grid.Row="0" Grid.Column="2" Width="40" Height="25" HorizontalAlignment="Left"  ToolTipService.ToolTip="Filtrar"                     >
            <Image Source="../Images/Search.png" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Stretch="UniformToFill" />
        </Button>
        
        <!-- ====================================================== -->

        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" >
            <TextBlock Text="[Dato agregado al ViewModel por dependency Inyection] Nombre del Proyecto: " HorizontalAlignment="Stretch" FontWeight="Bold"/>
            <TextBlock x:ConnectionId='5'                                                 HorizontalAlignment="Stretch"/>
        </StackPanel>
        

        <!-- ====================== CONTENIDO ===================== -->
        
        <!-- Para test binding -->


        <!-- <TextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Text="{x:Bind listas.VMListas, Mode=TwoWay}" HorizontalAlignment="Stretch" /> -->

        <RelativePanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" >
        <!--
            <ListView 
                ItemsSource="{x:Bind listasClaro.VMListas}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <Image Source="{Binding Image}" />
                            <TextBox Text="{Binding Title, Mode=TwoWay}" />  
                             <TextBlock Text="{Binding Title, Mode=TwoWay}" /> 
                        </StackPanel>
                    </DataTemplate>

                </ListView.ItemTemplate>
            </ListView>
            -->
            
            
            
            <GridView x:ConnectionId='4' x:Name="TodasLasListas" 
                      SelectionMode="Single"
                      IsItemClickEnabled="True" 
                      ItemTemplate="{StaticResource TodasLasListas_DefaultItemTemplate}" 
                                                          
                      >
            </GridView>

            
            
            
        </RelativePanel >
        
        
        
        
        
        
        
        <!-- ====================================================== -->


        <!-- ======================= FOOTER ======================= -->
        <!--
        <Button Grid.Row="2" Grid.Column="2" Width="40" Height="40"  HorizontalAlignment="Left" Click="ButtonTest_Click" Content="TEST" >

        </Button>
        -->
        <!-- ====================================================== -->
    </Grid>
</Page>

